import { MenuItem } from "../../logic/environment";
declare type PauseMenuProps = {
    title?: string;
    pauseMenuItems?: MenuItem[];
    dev?: boolean;
};
export default function PauseMenu(props: PauseMenuProps): JSX.Element;
export {};
