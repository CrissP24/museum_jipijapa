import { Reducer, Channel, Message } from "./index";
import { DataConnection } from "peerjs";
export declare class StreamChannel<Data = any, State = any> implements Channel {
    id: string;
    state: State;
    connections: Map<string, DataConnection>;
    reducer: Reducer;
    constructor(id: string, reducer: Reducer, connections: Map<string, DataConnection>);
    send(data: Data): void;
    receive(message: Message<Data>): void;
}
