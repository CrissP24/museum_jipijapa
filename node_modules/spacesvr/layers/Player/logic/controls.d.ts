import { MutableRefObject } from "react";
declare type PlayerControls = {
    lock: () => void;
    unlock: () => void;
    isLocked: () => boolean;
};
export declare const useControlLock: (lockControls: MutableRefObject<boolean>) => PlayerControls;
export {};
