import { ReactNode } from "react";
import { ConnectionConfig } from "./logic/connection";
export * from "./logic/network";
export declare type NetworkProps = {
    disableEntities?: boolean;
    autoconnect?: boolean;
} & ConnectionConfig;
declare type NetworkLayerProps = {
    children: ReactNode | ReactNode[];
} & NetworkProps;
export declare function Network(props: NetworkLayerProps): JSX.Element;
