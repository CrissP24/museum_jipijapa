import { GroupProps } from "@react-three/fiber";
declare type ShutterButton = {
    open: boolean;
    pressed: boolean;
    setPressed: (pressed: boolean) => void;
    onPress: () => void;
} & GroupProps;
export default function ShutterButton(props: ShutterButton): JSX.Element;
export {};
