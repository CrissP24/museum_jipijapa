import { MutableRefObject } from "react";
import { Clock, Vector3 } from "three";
export declare const useBob: (velocity: MutableRefObject<Vector3>, direction: MutableRefObject<Vector3>) => {
    update: (clock: Clock) => void;
};
