import { ConnectionState } from "./connection";
import { Signaller } from "./signallers";
export declare const useWaving: (minuteFrequency: number, singaller: Signaller | undefined, disconnect: ConnectionState["disconnect"]) => void;
