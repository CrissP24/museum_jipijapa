import { ReactNode } from "react";
declare type ToolPreloadProps = {
    setPreloadDone: (done: boolean) => void;
    children: ReactNode;
};
export default function ToolPreload(props: ToolPreloadProps): JSX.Element;
export {};
