import { ReactNode } from "react";
declare type FacePlayerProps = {
    children: ReactNode | ReactNode[];
    enabled?: boolean;
    lockX?: boolean;
    lockY?: boolean;
    lockZ?: boolean;
};
export declare function FacePlayer(props: FacePlayerProps): JSX.Element;
export {};
