import { Quaternion } from "three";
export declare class QuaterionSpring {
    velocity: Float32Array;
    stiffness: number;
    damping: number;
    constructor(damping?: number, stiffness?: number);
    _velLenSqr(): number;
    oscillationStep(cq: Quaternion, target: Quaternion, dt: number): void;
    criticallyStep(cq: Quaternion, target: Quaternion, dt: number): Quaternion | undefined;
}
