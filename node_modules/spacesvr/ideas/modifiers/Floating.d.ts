import { ReactNode } from "react";
declare type FloatingProps = {
    height?: number;
    speed?: number;
    children: ReactNode | ReactNode[];
};
export declare function Floating(props: FloatingProps): JSX.Element;
export {};
