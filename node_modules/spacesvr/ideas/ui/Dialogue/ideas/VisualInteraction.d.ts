import { Interaction } from "../logic/types";
declare type InteractionProps = {
    interaction: Interaction;
    enabled: boolean;
    width: number;
    height: number;
    setCurKey: (key: string) => void;
};
export default function VisualInteraction(props: InteractionProps): JSX.Element | null;
export {};
