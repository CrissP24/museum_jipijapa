import { ReactNode } from "react";
declare type Props = {
    children: ReactNode;
    xSpeed?: number;
    ySpeed?: number;
    zSpeed?: number;
};
export declare function Spinning(props: Props): JSX.Element;
export {};
