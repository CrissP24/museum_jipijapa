import { ReactNode } from "react";
declare type OnScreenProps = {
    distance: number;
    name: string;
    pos: [number, number];
    disableDraggable: boolean;
    children: ReactNode | ReactNode[];
};
export default function OnScreen(props: OnScreenProps): JSX.Element;
export {};
