import { DataConnection, Peer, MediaConnection } from "peerjs";
export declare const useVoiceConnections: (enabled: boolean, peer: Peer | undefined, connections: Map<string, DataConnection>, inputDeviceId?: string) => {
    mediaConnections: Map<string, MediaConnection>;
    localStream: MediaStream | undefined;
};
