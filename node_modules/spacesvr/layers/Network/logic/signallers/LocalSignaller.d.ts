import { Peer } from "peerjs";
import { Signaller } from "./";
declare type StoredPeer = {
    id: string;
    last_seen: number;
};
export declare class LocalSignaller implements Signaller {
    peerId: string;
    constructor(peer: Peer);
    readStore(): StoredPeer[];
    writeStore(peers: StoredPeer[]): void;
    cleanStore(): void;
    join(): Promise<string[] | undefined>;
    leave(): Promise<void>;
    wave(): Promise<boolean>;
}
export {};
