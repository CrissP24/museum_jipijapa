import { MutableRefObject, RefObject } from "react";
import { Group, Mesh, PerspectiveCamera } from "three";
import { Photography } from "./photo";
export declare const useRendering: (enabled: boolean, cam: MutableRefObject<PerspectiveCamera | undefined>, group: RefObject<Group>, mesh: RefObject<Mesh>, photo: Photography) => void;
