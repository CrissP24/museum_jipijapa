import { Clock } from "three";
import { RenderCallback } from "@react-three/fiber";
declare type Limiter = {
    isReady: (clock: Clock) => boolean;
};
export declare const useLimiter: (frequency: number) => Limiter;
export declare const useLimitedFrame: (frequency: number, callback: RenderCallback, renderPriority?: number) => void;
export {};
