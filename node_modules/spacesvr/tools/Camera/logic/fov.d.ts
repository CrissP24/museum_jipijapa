import { MutableRefObject } from "react";
import { PerspectiveCamera } from "three";
export declare type Fov = {
    val: number;
    normalized: number;
    set: (newVal: number) => void;
    min: number;
    max: number;
};
export declare const useFov: (cam: MutableRefObject<PerspectiveCamera | undefined>) => Fov;
