import { PerspectiveCamera, Vector2 } from "three";
export declare const getHudPos: (pos: [number, number] | Vector2, camera: PerspectiveCamera, distance: number, target?: Vector2) => Vector2;
export declare const getHudDims: (camera: PerspectiveCamera, distance: number) => {
    width: number;
    height: number;
};
export declare const useHudDims: (distance?: number) => {
    width: number;
    height: number;
};
